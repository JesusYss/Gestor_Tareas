# Configuración de SQL Server (PARA AUTENTICACIÓN DE WINDOWS)
DB_SERVER=LAPTOP-UVMVPLED     # Este es el "Server name" de tu SSMS
DB_DATABASE=GestorTareasDB    # Este es el "Name" de tu base de datos

# Estas opciones de cifrado son importantes porque tu SSMS las muestra activas
DB_ENCRYPT=true
DB_TRUST_SERVER_CERTIFICATE=true

# NOTA: Para autenticación de Windows, no necesitas DB_USER ni DB_PASSWORD en el .env
# Si los incluyes, mssql los ignorará si trustedConnection: true está en db.js